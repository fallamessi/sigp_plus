import 'package:flutter/material.dart';import '../../../core/widgets/pro_widgets.dart';
class SettingsPage extends StatefulWidget{const SettingsPage({super.key});@override State<SettingsPage> createState()=>_SettingsPageState();}
class _SettingsPageState extends State<SettingsPage>{bool realtime=true,offline=true,biometric=false;@override Widget build(BuildContext context)=>ListView(padding:const EdgeInsets.all(24),children:[const PageHeader(title:'Paramètres',subtitle:'Sécurité, synchronisation, notifications et préférences de poste.'),const SizedBox(height:18),Card(child:Padding(padding:const EdgeInsets.all(18),child:Column(children:[SwitchListTile(value:realtime,onChanged:(v)=>setState(()=>realtime=v),title:const Text('Notifications Realtime'),subtitle:const Text('Recevoir les changements immédiatement')),SwitchListTile(value:offline,onChanged:(v)=>setState(()=>offline=v),title:const Text('Mode Offline First'),subtitle:const Text('File locale, reprise et résolution des conflits')),SwitchListTile(value:biometric,onChanged:(v)=>setState(()=>biometric=v),title:const Text('Verrouillage renforcé'),subtitle:const Text('Demander une vérification après inactivité')),const Divider(),const ListTile(leading:Icon(Icons.security),title:Text('Expiration de session'),subtitle:Text('15 minutes d’inactivité')),const ListTile(leading:Icon(Icons.info_outline),title:Text('Version'),subtitle:Text('SIGP+ Pro statique 2.0.0'))])))]);}
